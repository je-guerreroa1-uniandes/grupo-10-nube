version: '3.1'

services:

  api:
    build:
      context: ../api
      dockerfile: Dockerfile
    ports:
      - '4000:5000'
    volumes:
      - uploads-volume:/app/uploads

  jobs:
    build:
      context: ../jobs
      dockerfile: Dockerfile
    volumes:
      - uploads-volume:/app/uploads

volumes:

  uploads-volume: